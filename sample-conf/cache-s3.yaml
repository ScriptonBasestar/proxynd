# S3 캐시 백엔드 설정 예시
cache:
  backend: s3
  s3:
    bucket: proxynd-cache
    prefix: cache
    region: ap-northeast-2
    # MinIO 등 S3 호환 서비스 사용 시
    # endpoint: http://localhost:9000
    
  # 캐시 옵션
  options:
    default_ttl: 3600  # 1시간
    max_size: 10737418240  # 10GB