# Phase 3: 관측성 정리 TODO

## 개요
- **목표**: 루트의 `logging/`, `metrics/`를 `internal/`로 일원화하여 중복 제거 및 구조 정리
- **우선순위**: High
- **예상 소요시간**: 5시간
- **담당자**: Backend/Fullstack

## 선행 작업
- [ ] 02-app-code-internalize.md 완료

## 세부 작업 목록

### 1. 브랜치 생성 및 환경 준비
- [ ] **브랜치 생성** (`git checkout -b refactor/observability-consolidation`)
  - 새 작업 브랜치 생성
  - 완료 기준: 브랜치 생성 및 최신 상태 확인
  - 주의사항: 이전 단계 완료 후 최신 develop 기반

### 2. 기존 구조 분석 및 충돌 해결 계획
- [ ] **기존 internal/logging 분석**
  - 루트 `logging/`과 `internal/logging/` 간 차이점 분석
  - 완료 기준: 파일별 차이점 문서화 및 병합 전략 수립
  - 주의사항: 중복 타입/함수 식별 및 충돌 해결 방안 계획

- [ ] **기존 internal/metrics 존재 여부 확인**
  - `internal/metrics/` 디렉토리 존재 시 충돌 분석
  - 완료 기준: 충돌 파일 목록 작성 및 병합 계획
  - 주의사항: 메트릭 네임스페이스 중복 방지

### 3. 디렉토리 생성 및 구조 준비
- [ ] **대상 디렉토리 확인** (`mkdir -p internal/logging internal/metrics`)
  - 병합 대상 디렉토리 준비
  - 완료 기준: 디렉토리 존재 확인
  - 주의사항: 기존 내용과의 병합 준비

### 4. 로깅 모듈 이동 및 병합
- [ ] **logging 파일 충돌 분석**
  - 루트 `logging/`과 `internal/logging/`의 파일별 비교
  - 완료 기준: diff 결과 분석 및 병합 대상 파일 선별
  - 주의사항: 동일 기능의 중복 구현 식별

- [ ] **logging 파일 이동** (`logging/*` → `internal/logging/`)
  - 충돌하지 않는 파일 우선 이동
  - 완료 기준: 비충돌 파일 이동 완료
  - 주의사항: Git 히스토리 보존을 위해 `git mv` 사용

- [ ] **logging 충돌 파일 병합**
  - 중복 기능 파일들의 수동 병합
  - 완료 기준: 기능 중복 없이 통합된 로깅 모듈
  - 주의사항: 로거 인터페이스 통일 및 초기화 로직 정리

### 5. 메트릭 모듈 이동 및 정리
- [ ] **metrics 파일 이동** (`metrics/*` → `internal/metrics/`)
  - 모든 메트릭 관련 파일을 internal로 이동
  - 완료 기준: 루트 metrics 디렉토리 비움
  - 주의사항: 기존 internal/metrics와 충돌 시 병합

- [ ] **메트릭 네임스페이스 정리**
  - Prometheus 메트릭 이름 중복 확인 및 정리
  - 완료 기준: 메트릭 이름 충돌 없음
  - 주의사항: 기존 모니터링 대시보드와의 호환성 유지

### 6. Import 경로 수정
- [ ] **logging import 경로 수정**
  - `grep -RIl "\bproxynd/logging\b" . | xargs -I{} sed -i '' -e 's|proxynd/logging|proxynd/internal/logging|g' {}`
  - 모든 로깅 모듈 참조 경로 업데이트
  - 완료 기준: 이전 경로 참조 완전 제거

- [ ] **metrics import 경로 수정**
  - `grep -RIl "\bproxynd/metrics\b" . | xargs -I{} sed -i '' -e 's|proxynd/metrics|proxynd/internal/metrics|g' {}`
  - 모든 메트릭 모듈 참조 경로 업데이트
  - 완료 기준: 이전 경로 참조 완전 제거

### 7. 글로벌 로거 초기화 통합
- [ ] **로거 초기화 로직 통합**
  - 중복된 로거 초기화 코드 제거 및 단일 진입점 생성
  - 완료 기준: 하나의 로거 초기화 함수로 통합
  - 주의사항: zap/zerolog 설정 통일 및 전역 로거 주입 방식 정리

- [ ] **로깅 설정 표준화**
  - 로그 레벨, 포맷, 출력 대상 설정 통일
  - 완료 기준: 일관된 로그 형식 및 설정 체계
  - 주의사항: JSON/텍스트 포맷 지원 유지

### 8. 메트릭 수집기 통합
- [ ] **메트릭 수집기 중복 제거**
  - 동일 메트릭을 수집하는 중복 코드 정리
  - 완료 기준: 메트릭별 단일 수집기 유지
  - 주의사항: 메트릭 정확성 및 성능 영향 최소화

- [ ] **Prometheus 엔드포인트 검증**
  - `/metrics` 엔드포인트 정상 동작 확인
  - 완료 기준: 메트릭 데이터 정상 노출
  - 주의사항: 메트릭 레이블 및 값 정확성 확인

## 완료 검증

### 1. 빌드 및 테스트 검증
- [ ] **코드 포맷팅** (`go fmt ./...`)
  - 모든 Go 파일 포맷팅
  - 완료 기준: 포맷팅 변경사항 없음

- [ ] **정적 분석** (`go vet ./...`)
  - 정적 분석 통과
  - 완료 기준: vet 오류 없음

- [ ] **린팅** (`golangci-lint run` - 선택사항)
  - 코드 품질 검증
  - 완료 기준: 린트 오류 해결

- [ ] **빌드 테스트** (`go build ./...`)
  - 전체 프로젝트 빌드
  - 완료 기준: 빌드 성공

- [ ] **단위 테스트** (`go test ./...`)
  - 모든 단위 테스트 실행
  - 완료 기준: 테스트 통과

### 2. 로깅 시스템 검증
- [ ] **로그 출력 테스트**
  - 서버 기동 후 로그 정상 출력 확인
  - 완료 기준: 구조화된 로그 형식으로 출력
  - 주의사항: JSON/텍스트 포맷 모두 지원 확인

- [ ] **로그 레벨 동작 확인**
  - 다양한 로그 레벨(debug, info, warn, error) 동작 확인
  - 완료 기준: 설정된 레벨에 따른 필터링 동작
  - 주의사항: 환경별 로그 레벨 설정 확인

### 3. 메트릭 시스템 검증
- [ ] **메트릭 엔드포인트 테스트** (`curl http://localhost:8080/metrics`)
  - Prometheus 메트릭 엔드포인트 동작 확인
  - 완료 기준: 메트릭 데이터 정상 응답
  - 주의사항: 메트릭 형식 및 레이블 정확성 확인

- [ ] **주요 메트릭 수집 확인**
  - HTTP 요청, 캐시 히트율, 시스템 리소스 등 주요 메트릭 수집 확인
  - 완료 기준: 핵심 메트릭들이 정상 수집됨
  - 주의사항: 메트릭 값의 정확성 및 업데이트 주기 확인

### 4. 모니터링 호환성 검증
- [ ] **기존 대시보드 호환성 확인**
  - Grafana 대시보드 등 기존 모니터링 도구와의 호환성 확인
  - 완료 기준: 기존 대시보드 정상 동작
  - 주의사항: 메트릭 이름 변경 시 대시보드 업데이트 필요

## 관련 파일
- `logging/` (이동/병합 대상)
- `metrics/` (이동 대상)
- `internal/logging/` (병합 대상)
- `internal/metrics/` (병합 대상)
- 모든 Go 소스 파일 (import 경로 수정 대상)
- `main.go` (로깅 초기화 코드)
- `monitoring/` (모니터링 설정 - 후속 작업에서 이동 예정)

## 롤백 계획
```bash
git restore --staged -W .
git checkout -- .
git checkout -
git branch -D refactor/observability-consolidation
```

## 완료 후 상태
- [ ] 루트의 `logging/`, `metrics/` 디렉토리 제거
- [ ] `internal/logging/`, `internal/metrics/`로 일원화 완료
- [ ] 중복 구현 제거 및 단일 진입점 확보
- [ ] 모든 import 경로 업데이트 완료
- [ ] 로깅 시스템 정상 동작
- [ ] 메트릭 수집 및 노출 정상 동작
- [ ] 기존 모니터링 시스템과의 호환성 유지